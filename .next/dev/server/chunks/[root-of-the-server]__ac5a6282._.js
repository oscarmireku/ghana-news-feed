module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/string_decoder [external] (string_decoder, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("string_decoder", () => require("string_decoder"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/node:assert [external] (node:assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:assert", () => require("node:assert"));

module.exports = mod;
}),
"[externals]/node:net [external] (node:net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:net", () => require("node:net"));

module.exports = mod;
}),
"[externals]/node:http [external] (node:http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http", () => require("node:http"));

module.exports = mod;
}),
"[externals]/node:querystring [external] (node:querystring, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:querystring", () => require("node:querystring"));

module.exports = mod;
}),
"[externals]/node:events [external] (node:events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:events", () => require("node:events"));

module.exports = mod;
}),
"[externals]/node:diagnostics_channel [external] (node:diagnostics_channel, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:diagnostics_channel", () => require("node:diagnostics_channel"));

module.exports = mod;
}),
"[externals]/node:util [external] (node:util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util", () => require("node:util"));

module.exports = mod;
}),
"[externals]/node:tls [external] (node:tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:tls", () => require("node:tls"));

module.exports = mod;
}),
"[externals]/node:buffer [external] (node:buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:buffer", () => require("node:buffer"));

module.exports = mod;
}),
"[externals]/node:zlib [external] (node:zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:zlib", () => require("node:zlib"));

module.exports = mod;
}),
"[externals]/node:perf_hooks [external] (node:perf_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:perf_hooks", () => require("node:perf_hooks"));

module.exports = mod;
}),
"[externals]/node:util/types [external] (node:util/types, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util/types", () => require("node:util/types"));

module.exports = mod;
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/node:worker_threads [external] (node:worker_threads, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:worker_threads", () => require("node:worker_threads"));

module.exports = mod;
}),
"[externals]/node:http2 [external] (node:http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http2", () => require("node:http2"));

module.exports = mod;
}),
"[externals]/node:async_hooks [external] (node:async_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:async_hooks", () => require("node:async_hooks"));

module.exports = mod;
}),
"[externals]/node:console [external] (node:console, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:console", () => require("node:console"));

module.exports = mod;
}),
"[externals]/node:fs/promises [external] (node:fs/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:fs/promises", () => require("node:fs/promises"));

module.exports = mod;
}),
"[externals]/node:path [external] (node:path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:path", () => require("node:path"));

module.exports = mod;
}),
"[externals]/node:timers [external] (node:timers, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:timers", () => require("node:timers"));

module.exports = mod;
}),
"[externals]/node:dns [external] (node:dns, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:dns", () => require("node:dns"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[project]/news/src/lib/db.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "db",
    ()=>db,
    "deleteInvalidArticles",
    ()=>deleteInvalidArticles,
    "deleteOldArticles",
    ()=>deleteOldArticles,
    "getAllArticles",
    ()=>getAllArticles,
    "getArticleCount",
    ()=>getArticleCount,
    "insertArticles",
    ()=>insertArticles
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f40$libsql$2f$client__$5b$external$5d$__$2840$libsql$2f$client$2c$__esm_import$2c$__$5b$project$5d2f$news$2f$node_modules$2f40$libsql$2f$client$29$__ = __turbopack_context__.i("[externals]/@libsql/client [external] (@libsql/client, esm_import, [project]/news/node_modules/@libsql/client)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$externals$5d2f40$libsql$2f$client__$5b$external$5d$__$2840$libsql$2f$client$2c$__esm_import$2c$__$5b$project$5d2f$news$2f$node_modules$2f40$libsql$2f$client$29$__
]);
[__TURBOPACK__imported__module__$5b$externals$5d2f40$libsql$2f$client__$5b$external$5d$__$2840$libsql$2f$client$2c$__esm_import$2c$__$5b$project$5d2f$news$2f$node_modules$2f40$libsql$2f$client$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
const url = process.env.TURSO_DATABASE_URL || 'file:news.db';
const authToken = process.env.TURSO_AUTH_TOKEN;
const db = (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$libsql$2f$client__$5b$external$5d$__$2840$libsql$2f$client$2c$__esm_import$2c$__$5b$project$5d2f$news$2f$node_modules$2f40$libsql$2f$client$29$__["createClient"])({
    url,
    authToken
});
// Initialize database (Async wrapper needed or call this explicitly if needed, 
// using top-level await if environment supports it, but simpler to invoke lazily or just ensure it exists.)
// For simplicity in Next.js Serverless, we can attempt to init on load or just cache the promise.
// Actually, pure SQL CREATE TABLE IF NOT EXISTS is cheap.
async function initDb() {
    await db.execute(`
    CREATE TABLE IF NOT EXISTS articles (
      id TEXT PRIMARY KEY,
      source TEXT NOT NULL,
      title TEXT NOT NULL,
      link TEXT UNIQUE NOT NULL,
      image TEXT,
      time TEXT NOT NULL,
      section TEXT NOT NULL,
      timestamp INTEGER NOT NULL,
      created_at INTEGER NOT NULL,
      content TEXT
    )
  `);
}
// We can just run this. in a serverless env it might run multiple times but it's safe.
// Ideally we'd await this before other ops.
initDb().catch(console.error);
async function insertArticles(articles) {
    if (articles.length === 0) return 0;
    let count = 0;
    // LibSQL client doesn't support massive bulk inserts in one statement easily without constructing the string manually
    // or using transactions nicely. 
    // We can use a transaction.
    const tx = await db.transaction('write');
    try {
        for (const article of articles){
            await tx.execute({
                sql: `
          INSERT INTO articles (id, source, title, link, image, time, section, timestamp, created_at, content)
          VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
          ON CONFLICT(link) DO UPDATE SET
            time = excluded.time,
            timestamp = excluded.timestamp,
            image = excluded.image,
            title = excluded.title,
            content = CASE WHEN excluded.content IS NOT NULL THEN excluded.content ELSE articles.content END
        `,
                args: [
                    article.id,
                    article.source,
                    article.title,
                    article.link,
                    article.image,
                    article.time,
                    article.section,
                    article.timestamp,
                    Date.now(),
                    article.content || null
                ]
            });
            count++;
        }
        await tx.commit();
        return count;
    } catch (err) {
        console.error('Error inserting articles:', err);
        // Don't rollback explicitly, LibSQL client handles it or it just fails.
        return 0;
    }
}
async function getAllArticles(limit = 500) {
    const rs = await db.execute({
        sql: `
      SELECT id, source, title, link, image, time, section, timestamp, content
      FROM articles
      WHERE image IS NOT NULL AND image != ''
      ORDER BY timestamp DESC
      LIMIT ?
    `,
        args: [
            limit
        ]
    });
    // rs.rows is correct
    return rs.rows;
}
async function getArticleCount() {
    const rs = await db.execute('SELECT COUNT(*) as count FROM articles');
    return Number(rs.rows[0].count);
}
async function deleteOldArticles(limit) {
    // SQLite doesn't support DELETE ... LIMIT directly in all versions, 
    // but simpler: DELETE WHERE id NOT IN (SELECT id ... LIMIT ?)
    const rs = await db.execute({
        sql: `
      DELETE FROM articles 
      WHERE id NOT IN (
        SELECT id FROM articles 
        ORDER BY timestamp DESC 
        LIMIT ?
      )
    `,
        args: [
            limit
        ]
    });
    return rs.rowsAffected;
}
async function deleteInvalidArticles() {
    const unwantedTitlePatterns = [
        'Home - News',
        'Home - Business',
        'Home - Sports',
        'Home-Business',
        'Business archive',
        'News Archive',
        'Sports Archive',
        'Photo Archives',
        'Archive',
        'Category:',
        'Section:',
        'More News',
        'More Stories',
        'View All',
        'Latest News',
        'Top Stories'
    ];
    let count = 0;
    try {
        const badImageResult = await db.execute(`
      DELETE FROM articles 
      WHERE image IS NULL 
         OR image = '' 
         OR image LIKE '%.svg%'
         OR image LIKE '%.svg?%'
    `);
        count += badImageResult.rowsAffected;
        // Construct OR conditions for titles
        if (unwantedTitlePatterns.length > 0) {
            const conditions = unwantedTitlePatterns.map(()=>`title LIKE ?`).join(' OR ');
            const args = unwantedTitlePatterns.map((p)=>`%${p}%`);
            const badTitleResult = await db.execute({
                sql: `DELETE FROM articles WHERE ${conditions}`,
                args: args
            });
            count += badTitleResult.rowsAffected;
        }
    } catch (err) {
        console.error('Error deleting invalid articles:', err);
    }
    return count;
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/news/src/app/api/news/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "GET",
    ()=>GET,
    "revalidate",
    ()=>revalidate
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/news/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/news/node_modules/cheerio/dist/esm/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/news/node_modules/cheerio/dist/esm/load-parse.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs [external] (fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/news/src/lib/db.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
const revalidate = 60; // Revalidate every 1 minute
// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------
function normalizeTitle(title) {
    return title.toLowerCase().replace(/[^a-z0-9]/g, '');
}
function resolveUrl(base, relative) {
    try {
        return new URL(relative, base).href;
    } catch  {
        return relative;
    }
}
// Parse publication date from various formats
function parsePublicationDate(dateStr) {
    if (!dateStr || dateStr.trim() === '') {
        return {
            timestamp: Date.now(),
            display: 'Recent'
        };
    }
    const cleaned = dateStr.trim();
    const d = new Date(cleaned);
    if (!isNaN(d.getTime())) {
        const timestamp = d.getTime();
        const display = d.toLocaleString('en-US', {
            hour: 'numeric',
            minute: '2-digit',
            month: 'short',
            day: 'numeric'
        });
        return {
            timestamp,
            display
        };
    }
    // If we can't parse it, return current time
    return {
        timestamp: Date.now(),
        display: 'Recent'
    };
}
function timeAgo(timestamp) {
    const seconds = Math.floor((Date.now() - timestamp) / 1000);
    let interval = seconds / 31536000; // years
    if (interval > 1) return Math.floor(interval) + " years ago";
    interval = seconds / 2592000; // months
    if (interval > 1) return Math.floor(interval) + " months ago";
    interval = seconds / 86400; // days
    if (interval > 1) return Math.floor(interval) + " days ago";
    interval = seconds / 3600; // hours
    if (interval > 1) return Math.floor(interval) + " hours ago";
    interval = seconds / 60; // minutes
    if (interval > 1) return Math.floor(interval) + " minutes ago";
    return Math.floor(seconds) + " seconds ago";
}
// Fallback: Fetch article page to find og:image and parsing date
async function fetchArticleMetadata(link, source) {
    try {
        // Special handling for PeaceFM API
        if (source === 'PeaceFM' || link.includes('peacefmonline.com')) {
            // Extract ID. format: /.../.../123456-some-slug
            const idMatch = link.match(/\/(\d+)-/);
            if (idMatch) {
                const id = idMatch[1];
                const apiUrl = `https://articles-engine.peacefmonline.com/v1/articles/${id}`;
                const res = await fetch(apiUrl, {
                    headers: {
                        'User-Agent': 'Mozilla/5.0'
                    }
                });
                if (res.ok) {
                    const json = await res.json();
                    if (json.status === 'success' && json.data) {
                        const d = json.data;
                        return {
                            image: d.main_photo?.photo_url || null,
                            timestamp: new Date(d.created_at).getTime(),
                            time: timeAgo(new Date(d.created_at).getTime()),
                            content: d.main_description // This is HTML
                        };
                    }
                }
            }
        }
        const res = await fetch(link, {
            headers: {
                'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36'
            }
        });
        if (!res.ok) return {
            image: null
        };
        const html = await res.text();
        const $ = __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](html);
        // Image
        const image = $('meta[property="og:image"]').attr('content') || $('meta[name="twitter:image"]').attr('content') || null;
        // Date
        let dateStr = $('meta[property="og:article:published_time"]').attr('content') || $('meta[property="article:published_time"]').attr('content') || $('meta[name="article:published_time"]').attr('content') || $('meta[property="og:updated_time"]').attr('content') || $('meta[property="article:modified_time"]').attr('content') || $('meta[itemprop="datePublished"]').attr('content') || $('meta[name="pubdate"]').attr('content') || $('#date').text() || $('time').first().attr('datetime');
        let timestamp;
        let time;
        if (dateStr) {
            const parsed = parsePublicationDate(dateStr);
            if (parsed.display !== 'Recent') {
                timestamp = parsed.timestamp;
                time = parsed.display;
            }
        }
        // Content Extraction
        let contentSelector = '';
        if (source === '3News') contentSelector = '.prose';
        else if (source === 'GhanaWeb') contentSelector = '#medsection1, .article-content-area';
        else if (source === 'AdomOnline') contentSelector = '.td-post-content';
        else if (source === 'MyJoyOnline') contentSelector = '#article-text';
        // Generic fallback selectors
        if (!contentSelector) {
            contentSelector = '.entry-content, .article-body, .post-content, .content-wrapper, article';
        }
        let contentEl = $(contentSelector).first();
        if (contentEl.length === 0 && source === 'GhanaWeb') {
            // Fallback for GhanaWeb
            contentEl = $('p').parent().filter((i, el)=>$(el).find('p').length > 3).first();
        }
        let content = '';
        if (contentEl.length) {
            // Remove unwanted elements
            contentEl.find('script, style, iframe, .related-posts, .ads, .ad, [class*="ad-"], [id*="ad-"]').remove();
            // Get paragraphs
            const paragraphs = [];
            contentEl.find('p').each((_, el)=>{
                const text = $(el).text().trim();
                if (text.length > 20) {
                    paragraphs.push(`<p>${text}</p>`);
                }
            });
            content = paragraphs.join('');
        }
        return {
            image,
            timestamp,
            time,
            content
        };
    } catch  {
        return {
            image: null
        };
    }
}
// ---------------------------------------------------------------------------
// Source: GhanaWeb (Scrape Sections)
// ---------------------------------------------------------------------------
async function scrapeGhanaWeb() {
    const sections = [
        {
            name: 'News',
            url: 'https://www.ghanaweb.com/GhanaHomePage/NewsArchive/'
        },
        {
            name: 'Sports',
            url: 'https://www.ghanaweb.com/GhanaHomePage/SportsArchive/'
        },
        {
            name: 'Business',
            url: 'https://www.ghanaweb.com/GhanaHomePage/business/'
        }
    ];
    const stories = [];
    const seenLinks = new Set();
    await Promise.all(sections.map(async (sec)=>{
        try {
            const res = await fetch(sec.url, {
                headers: {
                    'User-Agent': 'Mozilla/5.0'
                }
            });
            const html = await res.text();
            const $ = __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](html);
            // Strategy: 
            // 1. Try known containers (desktop view)
            // 2. Fallback: Find ALL links that look like articles in this section
            let candidates = $('.inner-lead, .sub-menu-list__news li, .news_listing .inner, #leads .inner, .news-list li');
            if (candidates.length === 0) {
                // Fallback: finding any link containing 'Article' or looking like a news path
                // GhanaWeb articles: /GhanaHomePage/NewsArchive/artikel.php?ID=... or similar slugs
                candidates = $('a[href*="artikel.php"], a[href*="/NewsArchive/"], a[href*="/SportsArchive/"], a[href*="/business/"]');
            }
            candidates.slice(0, 10).each((_, el)=>{
                const a = $(el).is('a') ? $(el) : $(el).find('a').first();
                const link = a.attr('href');
                let title = a.attr('title') || a.text().trim();
                if (!title || title.length < 5) {
                    title = $(el).find('h2, h3, h4, p').text().trim();
                }
                let image = $(el).find('img').attr('src');
                if (!image) {
                    // Check parent for image
                    image = $(el).closest('div, li').find('img').attr('src');
                }
                // Extract publication date - GhanaWeb typically has date in .date, .time, or time elements
                let dateStr = $(el).find('.date, .time, time, .published, .post-date').first().text().trim();
                if (!dateStr) {
                    // Check for datetime attribute in time element
                    dateStr = $(el).find('time').attr('datetime') || '';
                }
                const { timestamp, display } = parsePublicationDate(dateStr);
                // Filter out unwanted article types (navigation links, category pages, etc.)
                // These are typically navigation elements, not actual articles
                const unwantedTitlePatterns = [
                    'Home - News',
                    'Home - Business',
                    'Home - Sports',
                    'Home-Business',
                    'Business archive',
                    'News Archive',
                    'Sports Archive',
                    'Photo Archives',
                    'Archive',
                    'Category:',
                    'Section:',
                    'More News',
                    'More Stories',
                    'View All',
                    'Latest News',
                    'Top Stories'
                ];
                // Check if title matches unwanted patterns (case-insensitive)
                const isUnwantedTitle = unwantedTitlePatterns.some((pattern)=>title.toLowerCase().trim() === pattern.toLowerCase() || title.toLowerCase().includes(pattern.toLowerCase()));
                // Also exclude links that don't have a proper article ID
                // GhanaWeb articles must have artikel.php?ID= or a numeric ID in the URL
                const hasArticleId = link?.includes('artikel.php?ID=') || /\/\d{7,}-/.test(link || ''); // Matches URLs with 7+ digit IDs
                // Filter out articles with SVG images (these are usually placeholders/icons)
                const hasSvgImage = image?.toLowerCase().endsWith('.svg');
                if (link && title && title.length > 10 && !link.includes('javascript') && !link.includes('#') && !isUnwantedTitle && hasArticleId && !hasSvgImage) {
                    const fullLink = resolveUrl('https://www.ghanaweb.com', link);
                    if (seenLinks.has(fullLink)) return;
                    seenLinks.add(fullLink);
                    if (image) image = resolveUrl('https://cdn.ghanaweb.com', image);
                    stories.push({
                        id: `gw-${stories.length + Math.random()}`,
                        source: 'GhanaWeb',
                        title,
                        link: fullLink,
                        image: image || null,
                        time: display,
                        timestamp,
                        section: sec.name
                    });
                }
            });
        } catch (e) {
            console.error(`GhanaWeb ${sec.name} Error:`, e);
        }
    }));
    return stories;
}
// ---------------------------------------------------------------------------
// Source: AdomOnline (RSS Sections)
// ---------------------------------------------------------------------------
async function scrapeAdomOnline() {
    const feeds = [
        {
            name: 'News',
            url: 'https://www.adomonline.com/category/news/feed/'
        },
        {
            name: 'Sports',
            url: 'https://www.adomonline.com/category/sports/feed/'
        },
        {
            name: 'Business',
            url: 'https://www.adomonline.com/category/business/feed/'
        }
    ];
    const stories = [];
    const seenLinks = new Set();
    await Promise.all(feeds.map(async (feed)=>{
        try {
            const res = await fetch(feed.url, {
                headers: {
                    'User-Agent': 'Mozilla/5.0'
                }
            });
            const xml = await res.text();
            const $ = __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](xml, {
                xmlMode: true
            });
            $('item').slice(0, 5).each((_, el)=>{
                const title = $(el).find('title').text().trim();
                const link = $(el).find('link').text().trim();
                const pubDate = $(el).find('pubDate').text().trim();
                if (seenLinks.has(link)) return;
                seenLinks.add(link);
                let timestamp = Date.now();
                let timeDisplay = 'Recent';
                if (pubDate) {
                    const d = new Date(pubDate);
                    if (!isNaN(d.getTime())) {
                        timestamp = d.getTime();
                        timeDisplay = d.toLocaleString('en-US', {
                            hour: 'numeric',
                            minute: '2-digit',
                            month: 'short',
                            day: 'numeric'
                        });
                    }
                }
                let image = $(el).find('media\\:content').attr('url') || $(el).find('media\\:thumbnail').attr('url');
                if (!image) {
                    const content = $(el).find('content\\:encoded').text();
                    const match = content.match(/src="([^"]+)"/);
                    if (match) image = match[1];
                }
                stories.push({
                    id: `adom-${stories.length + Math.random()}`,
                    source: 'AdomOnline',
                    title,
                    link,
                    image: image || null,
                    time: timeDisplay,
                    timestamp,
                    section: feed.name
                });
            });
        } catch (e) {
            console.error(`AdomOnline ${feed.name} Error:`, e);
        }
    }));
    return stories;
}
// ---------------------------------------------------------------------------
// Source: PeaceFM (API)
// ---------------------------------------------------------------------------
async function scrapePeaceFM() {
    console.log('Scraping PeaceFM (API)...');
    try {
        const response = await fetch('https://articles-engine.peacefmonline.com/v1/articles?limit=30', {
            headers: {
                'User-Agent': 'Mozilla/5.0'
            }
        });
        if (!response.ok) {
            console.error(`PeaceFM API error: ${response.status}`);
            return [];
        }
        const json = await response.json();
        if (json.status !== 'success' || !json.data) return [];
        const articles = json.data.map((item)=>{
            // Construct URL: https://peacefmonline.com/[section]/[category]/[slug]
            const section = item.section?.section_location || 'local';
            const category = item.category?.category_location || 'news';
            const link = `https://www.peacefmonline.com/pages/${section}/${category}/${item.slug}`;
            return {
                id: `peace-${item.article_id}`,
                source: 'PeaceFM',
                title: item.main_title,
                link: link,
                image: item.main_photo?.photo_url || null,
                time: timeAgo(new Date(item.created_at).getTime()),
                section: item.section?.section_name || 'News',
                timestamp: new Date(item.created_at).getTime()
            };
        });
        return articles.filter((a)=>a.title && a.link);
    } catch (error) {
        console.error('Error scraping PeaceFM:', error);
        return [];
    }
}
// ---------------------------------------------------------------------------
// Source: MyJoyOnline (HTML Scrape Sections)
// ---------------------------------------------------------------------------
async function scrapeMyJoyOnline() {
    const sections = [
        {
            name: 'News',
            url: 'https://www.myjoyonline.com/news/'
        },
        {
            name: 'Sports',
            url: 'https://www.myjoyonline.com/sports/'
        },
        {
            name: 'Business',
            url: 'https://www.myjoyonline.com/business/'
        }
    ];
    const stories = [];
    const seenLinks = new Set();
    await Promise.all(sections.map(async (sec)=>{
        try {
            const res = await fetch(sec.url, {
                headers: {
                    'User-Agent': 'Mozilla/5.0'
                }
            });
            const html = await res.text();
            const $ = __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](html);
            const containers = $('.news-main-list .col-lg-3, .col-lg-6, .home-section-story-list li, .main-listing-article');
            containers.slice(0, 10).each((_, el)=>{
                const imgLink = $(el).find('a.bgposition');
                const titleLink = $(el).find('h3 a, h4 a, .title a').first();
                const fallbackLink = $(el).find('a').first();
                const finalLink = titleLink.length ? titleLink : imgLink.length ? imgLink : fallbackLink;
                const link = finalLink.attr('href');
                let title = finalLink.text().trim();
                if (!title) title = $(el).find('h1, h2, h3, h4').text().trim();
                let image = $(el).find('img').attr('data-src') || $(el).find('img').attr('src');
                if (!image && imgLink.attr('style')) {
                    const match = imgLink.attr('style')?.match(/url\(['"]?([^'"]+)['"]?\)/);
                    if (match) image = match[1];
                }
                let dateStr = $(el).find('.date, time, .post-date, .published, .entry-date').first().text().trim();
                if (!dateStr) {
                    dateStr = $(el).find('time').attr('datetime') || '';
                }
                const { timestamp, display } = parsePublicationDate(dateStr);
                if (link && title && title.length > 10) {
                    const fullLink = resolveUrl('https://www.myjoyonline.com', link);
                    if (seenLinks.has(fullLink)) return;
                    seenLinks.add(fullLink);
                    stories.push({
                        id: `joy-${stories.length + Math.random()}`,
                        source: 'MyJoyOnline',
                        title,
                        link: fullLink,
                        image: image || null,
                        time: display,
                        timestamp,
                        section: sec.name
                    });
                }
            });
        } catch (e) {
            console.error(`MyJoyOnline ${sec.name} Error:`, e);
        }
    }));
    return stories;
}
// ---------------------------------------------------------------------------
// Source: 3News (RSS Feed)
// ---------------------------------------------------------------------------
async function scrape3News() {
    const stories = [];
    const seenLinks = new Set();
    try {
        const res = await fetch('https://3news.com/news/feed.xml', {
            headers: {
                'User-Agent': 'Mozilla/5.0'
            }
        });
        if (!res.ok) throw new Error(`3News RSS failed: ${res.status}`);
        const xml = await res.text();
        const $ = __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](xml, {
            xmlMode: true
        });
        $('item').slice(0, 10).each((_, el)=>{
            const title = $(el).find('title').text().trim();
            const link = $(el).find('link').text().trim();
            const pubDate = $(el).find('pubDate').text().trim();
            if (seenLinks.has(link)) return;
            seenLinks.add(link);
            let timestamp = Date.now();
            let timeDisplay = 'Recent';
            if (pubDate) {
                const parsed = parsePublicationDate(pubDate);
                timestamp = parsed.timestamp;
                timeDisplay = parsed.display;
            }
            let image = $(el).find('media\\:content').attr('url') || $(el).find('media\\:thumbnail').attr('url');
            if (!image) {
                const description = $(el).find('description').text();
                const match = description.match(/src="([^"]+)"/);
                if (match) image = match[1];
            }
            stories.push({
                id: `3news-${stories.length + Math.random()}`,
                source: '3News',
                title,
                link,
                image: image || null,
                time: timeDisplay,
                timestamp,
                section: 'News'
            });
        });
    } catch (e) {
        console.error('3News Error:', e);
    }
    return stories;
}
async function scrapeDailyGuide() {
    const stories = [];
    const logs = [];
    try {
        const res = await fetch('https://dailyguidenetwork.com/feed/', {
            headers: {
                'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36'
            },
            next: {
                revalidate: 300
            } // Cache for 5 minutes
        });
        if (!res.ok) {
            logs.push(`DailyGuide RSS Error: ${res.status}`);
            return {
                stories,
                logs
            };
        }
        const xml = await res.text();
        const $ = __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](xml, {
            xmlMode: true
        });
        const items = $('item').slice(0, 10);
        logs.push(`DailyGuide: Found ${items.length} items`);
        items.each((_, el)=>{
            const title = $(el).find('title').text().trim();
            const link = $(el).find('link').text().trim();
            const pubDate = $(el).find('pubDate').text().trim();
            if (!link) {
                logs.push(`DailyGuide: Skipped item with no link: ${title}`);
                return;
            }
            let timestamp = Date.now();
            let timeDisplay = 'Recent';
            if (pubDate) {
                const parsed = parsePublicationDate(pubDate);
                timestamp = parsed.timestamp;
                timeDisplay = parsed.display;
            }
            stories.push({
                id: `dailyguide-${stories.length + Math.random()}`,
                source: 'DailyGuide',
                title,
                link,
                image: null,
                time: timeDisplay,
                timestamp,
                section: 'News'
            });
        });
    } catch (e) {
        logs.push(`DailyGuide Error: ${e}`);
    }
    return {
        stories,
        logs
    };
}
// ---------------------------------------------------------------------------
// Source: Generic RSS Scraper (Additional Top Sources)
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// Source: Generic RSS Scraper (Additional Top Sources)
// ---------------------------------------------------------------------------
const GENERIC_FEEDS = [
    {
        source: 'CitiNewsRoom',
        url: 'https://citinewsroom.com/feed/',
        section: 'News'
    },
    {
        source: 'Modern Ghana',
        url: 'https://www.modernghana.com/rssfeed/news.xml',
        section: 'News'
    },
    {
        source: 'GNA',
        url: 'https://gna.org.gh/feed/',
        section: 'News'
    },
    {
        source: 'Graphic Online',
        url: 'https://www.graphic.com.gh/news/general-news?format=feed',
        section: 'News'
    },
    {
        source: 'Ghanaian Times',
        url: 'https://www.ghanaiantimes.com.gh/feed/',
        section: 'News'
    },
    {
        source: 'Starr FM',
        url: 'https://starrfm.com.gh/feed/',
        section: 'News'
    },
    {
        source: 'News Ghana',
        url: 'https://newsghana.com.gh/feed/',
        section: 'News'
    },
    {
        source: 'The B&FT',
        url: 'https://thebftonline.com/feed/',
        section: 'Business'
    },
    {
        source: 'Atinka Online',
        url: 'https://atinkaonline.com/feed/',
        section: 'News'
    },
    {
        source: 'Asaase Radio',
        url: 'https://asaaseradio.com/feed/',
        section: 'News'
    },
    {
        source: 'The Herald',
        url: 'https://theheraldghana.com/feed/',
        section: 'News'
    },
    {
        source: 'The Chronicle',
        url: 'https://thechronicle.com.gh/feed/',
        section: 'News'
    },
    {
        source: 'GhPage',
        url: 'https://ghpage.com/feed/',
        section: 'Entertainment'
    },
    {
        source: 'Ameyaw Debrah',
        url: 'https://ameyawdebrah.com/feed/',
        section: 'Entertainment'
    },
    {
        source: 'YFM Ghana',
        url: 'https://yfmghana.com/feed/',
        section: 'Entertainment'
    },
    {
        source: 'Happy Ghana',
        url: 'https://www.happyghana.com/feed/',
        section: 'News'
    },
    {
        source: 'GhanaSoccerNet',
        url: 'https://ghanasoccernet.com/feed',
        section: 'Sports'
    }
];
async function scrapeGenericRSS() {
    const stories = [];
    await Promise.all(GENERIC_FEEDS.map(async (feed)=>{
        try {
            const res = await fetch(feed.url, {
                headers: {
                    'User-Agent': 'Mozilla/5.0'
                },
                next: {
                    revalidate: 300
                }
            });
            if (!res.ok) return;
            const xml = await res.text();
            const $ = __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](xml, {
                xmlMode: true
            });
            let items = $('item');
            let isAtom = false;
            if (items.length === 0) {
                items = $('entry');
                isAtom = true;
            }
            items.slice(0, 5).each((_, el)=>{
                const title = $(el).find('title').text().trim();
                let link = '';
                if (isAtom) {
                    link = $(el).find('link').attr('href') || $(el).find('link').text().trim();
                } else {
                    link = $(el).find('link').text().trim();
                    if (!link) link = $(el).find('guid').text().trim();
                }
                let pubDate = '';
                if (isAtom) {
                    pubDate = $(el).find('published').text() || $(el).find('updated').text();
                } else {
                    pubDate = $(el).find('pubDate').text().trim();
                }
                if (!link) return;
                let timestamp = Date.now();
                let timeDisplay = 'Recent';
                if (pubDate) {
                    const parsed = parsePublicationDate(pubDate);
                    timestamp = parsed.timestamp;
                    timeDisplay = parsed.display;
                }
                // Try to find image
                let image = $(el).find('media\\:content').attr('url')?.trim() || $(el).find('media\\:thumbnail').attr('url')?.trim() || $(el).find('enclosure').attr('url')?.trim();
                if (!image) {
                    // Try content for image
                    const content = $(el).find('content\\:encoded').text() || $(el).find('content').text() || $(el).find('description').text();
                    const match = content.match(/src="([^"]+)"/);
                    if (match) image = match[1].trim();
                }
                // Extract category
                let category = '';
                if (isAtom) {
                    category = $(el).find('category').attr('term') || $(el).find('category').attr('label') || '';
                } else {
                    category = $(el).find('category').first().text().trim();
                }
                // Clean up category
                if (category.toLowerCase().includes('news')) category = 'News';
                else if (category.length > 20) category = category.substring(0, 20) + '...';
                stories.push({
                    id: `${feed.source.toLowerCase().replace(/\s+/g, '')}-${stories.length + Math.random()}`,
                    source: feed.source,
                    title,
                    link,
                    image: image || null,
                    time: timeDisplay,
                    timestamp,
                    section: category || feed.section
                });
            });
        } catch (e) {
            console.error(`Generic RSS ${feed.source} Error:`, e);
        }
    }));
    return stories;
}
async function GET(request) {
    // Parse query parameters
    const { searchParams } = new URL(request.url);
    const pageParam = searchParams.get('page');
    const limitParam = searchParams.get('limit');
    // Default to page 1, limit 20 (or use existing behavior if not specified? 
    // The plan said: "Clients without parameters will still receive the default list" 
    // But for infinite scroll, default list usually means page 1.
    // Let's stick to consistent pagination. Default to all/200 if not specified might be better for backward compat,
    // but user asked for pagination support. Let's make it optional.
    const page = pageParam ? parseInt(pageParam) : 1;
    // If limit is not specified, return 20 as per modern standards, or kept 200? 
    // User asked for "pagination and infinite scroll support". 
    // Usually implies defaulting to a smaller set. Let's default to 20.
    const limit = limitParam ? parseInt(limitParam) : 20;
    const [ghanaStories, adomStories, peaceStories, joyStories, threeNewsStories, dailyGuideResult, genericStories] = await Promise.all([
        scrapeGhanaWeb(),
        scrapeAdomOnline(),
        scrapePeaceFM(),
        scrapeMyJoyOnline(),
        scrape3News(),
        scrapeDailyGuide(),
        scrapeGenericRSS()
    ]);
    const dailyGuideStories = dailyGuideResult.stories;
    const debugLogs = dailyGuideResult.logs;
    let allStories = [
        ...ghanaStories,
        ...adomStories,
        ...peaceStories,
        ...joyStories,
        ...threeNewsStories,
        ...dailyGuideStories,
        ...genericStories
    ];
    // Deduplicate by link
    const uniqueMap = new Map();
    allStories.forEach((s)=>uniqueMap.set(s.link, s));
    allStories = Array.from(uniqueMap.values());
    // sort
    allStories.sort((a, b)=>b.timestamp - a.timestamp);
    // Deep Fetch Metadata for ALL articles to ensure correct images and dates
    // Process in batches to avoid overwhelming the servers
    const batch = allStories.slice(0, 80); // Limit to 80 articles per scrape
    console.log(`Fetching metadata for ${batch.length} articles...`);
    await Promise.all(batch.map(async (story)=>{
        const metadata = await fetchArticleMetadata(story.link, story.source);
        // Always update with metadata from article page if available
        if (metadata.timestamp && metadata.time) {
            story.timestamp = metadata.timestamp;
            story.time = metadata.time;
        }
        if (metadata.image) {
            story.image = metadata.image; // Always use image from article page
        }
        if (metadata.content) {
            story.content = metadata.content;
        }
    }));
    // Final Sort by timestamp descending (newest first)
    allStories.sort((a, b)=>b.timestamp - a.timestamp);
    // Clean up all image URLs (trim whitespace)
    allStories.forEach((story)=>{
        if (story.image) {
            story.image = story.image.trim();
        }
    });
    // Filter out articles without images AND articles with SVG images (placeholders/icons)
    const storiesWithImages = allStories.filter((story)=>story.image !== null && story.image !== '' && !story.image.toLowerCase().endsWith('.svg'));
    console.log(`Filtered: ${allStories.length} total -> ${storiesWithImages.length} with valid images (excluded SVGs)`);
    // Save new articles to database (only those with images)
    const newArticlesCount = (0, __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["insertArticles"])(storiesWithImages);
    console.log(`Added ${newArticlesCount} new articles to database`);
    // Clean up database: remove invalid articles and limit total count
    const deletedInvalid = (0, __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["deleteInvalidArticles"])();
    const deletedOld = (0, __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["deleteOldArticles"])(500);
    console.log(`Cleaned up DB: Removed ${deletedInvalid} invalid and ${deletedOld} old articles.`);
    // Get all articles from database 
    // Now we can just request the limit we enforced, or slightly less for pagination if needed.
    // But user wants "limit the database articles to 500", so fetching 500 is correct.
    let dbArticles = (0, __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getAllArticles"])(500);
    const totalCount = (0, __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$src$2f$lib$2f$db$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getArticleCount"])();
    // Remove timestamp field from output (keep only the display time)
    const storiesOutput = dbArticles.map(({ timestamp, ...story })=>story);
    const data = {
        generated_at: new Date().toISOString(),
        count: storiesOutput.length,
        total_in_database: totalCount,
        new_articles_added: newArticlesCount,
        stories: storiesOutput,
        debug_logs: debugLogs
    };
    try {
        const publicDir = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'public');
        if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(publicDir)) __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].mkdirSync(publicDir);
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].writeFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(publicDir, 'news-feed.json'), JSON.stringify(data, null, 2));
    } catch (err) {
        console.error('Failed to save content JSON:', err);
    }
    return __TURBOPACK__imported__module__$5b$project$5d2f$news$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(data);
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__ac5a6282._.js.map